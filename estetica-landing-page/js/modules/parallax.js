/**
 * Parallax Module
 * Propósito: Efecto parallax suave usando requestAnimationFrame
 * Dependencias: Ninguna
 * Nota: Deshabilitado por request del usuario
 */

export function initParallax() {
  // Deshabilitado
  return;
}